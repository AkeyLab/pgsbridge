% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/traits.R
\name{sample_causal_variants}
\alias{sample_causal_variants}
\title{Draw causal variants spread evenly across a variant pool}
\usage{
sample_causal_variants(n_pool, m_causal)
}
\arguments{
\item{n_pool}{Number of variants in the pool.}

\item{m_causal}{Number of causal variants to draw.}
}
\value{
An integer vector of \code{m_causal} column indices into the pool.
}
\description{
The pool of \code{n_pool} variants is cut into \code{m_causal} consecutive blocks of
width \code{floor(n_pool / m_causal)}, and one variant is drawn uniformly at
random from each block. Causal variants are therefore separated by
\code{floor(n_pool / m_causal)} variants on average. For the pools used here that
runs from a few hundred variants, for 1,000 causal variants drawn from the
common pool, to tens of thousands, for 20 causal variants drawn from the same
pool, so that any two of them sit far apart in the genome and carry
negligible linkage disequilibrium. Individual separations vary:
two consecutive draws can land at the end of one block and the start of the
next, so the spacing is a mean rather than a guaranteed minimum.
}
