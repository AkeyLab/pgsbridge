% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theory.R
\name{shared_effect_size}
\alias{shared_effect_size}
\title{Effect size shared by all causal variants achieving a target heritability}
\usage{
shared_effect_size(h2, tau2, p)
}
\arguments{
\item{h2}{The target heritability, in \verb{(0, 1)}.}

\item{tau2}{The non-genetic variance.}

\item{p}{The reference-allele frequencies of the causal variants.}
}
\value{
The single effect size shared by every causal variant.
}
\description{
With a single effect size \code{beta} shared by all \code{m} causal variants the
additive genetic variance is \verb{2 beta^2 sum_i p_i (1 - p_i)}, so reaching
heritability \code{h2} at non-genetic variance \code{tau^2} in a Hardy-Weinberg
population requires the effect size below.
}
\details{
\verb{beta = sqrt( h2 / (1 - h2) * tau^2 / (2 sum_i p_i (1 - p_i)) )}.
}
\examples{
p <- runif(100, 0.05, 0.95)
beta <- shared_effect_size(h2 = 0.5, tau2 = 1, p = p)
# the realised heritability matches the target
prediction_accuracy(
    sigma2 = additive_genetic_variance(rep(beta, length(p)), p),
    phi_bar = 0.5, tau2 = 1
)

}
