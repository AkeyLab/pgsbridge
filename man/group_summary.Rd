% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summaries.R
\name{group_summary}
\alias{group_summary}
\title{Group means and confidence intervals, after trimming outlying replicates}
\usage{
group_summary(data, value, groups, level = 0.95)
}
\arguments{
\item{data}{A data frame of replicate-level results.}

\item{value}{Name of the column holding the value to summarise.}

\item{groups}{Character vector of column names defining the groups.}

\item{level}{Confidence level. Defaults to 0.95.}
}
\value{
A data frame with one row per group, holding the grouping columns and
the columns \code{n} (kept replicates), the mean under its original name, \code{sd},
\code{se}, and \code{ci} (the confidence interval half-width).
}
\description{
Transferability is a ratio of two estimated accuracies. When the denominator
happens to be small in a replicate, the ratio can take an extreme value, and
a handful of such replicates would dominate an ordinary mean. Within each
group the replicates are therefore trimmed once by the interquartile rule:
with q1 and q3 the first and third quartiles and iqr = q3 - q1, a replicate is
kept if it lies in \verb{[q1 - 1.5 iqr, q3 + 1.5 iqr]}. The mean, the standard
error, and the confidence interval are computed from the kept replicates.
}
\details{
The confidence interval half-width is qt(1 - (1 - level)/2, N - 1) times the
standard error, where N is the number of kept replicates in the group.
}
